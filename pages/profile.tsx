import CardProfile from "../src/components/card-profile";
import { Container, Form } from "react-bootstrap";

const Register = () => {
  return (
    <Container>
      <CardProfile />
    </Container>
  );
};

export default Register;
